<?xml version="1.0"?>
<!-- The COPYRIGHT file at the top level of this repository contains the full
     copyright notices and license terms. -->
<tryton>
    <data>
        <!-- TODO: Remove after 6.0 upgrade as we just keep it to prevent crashes on upgrades -->
        <record model="ir.sequence.type" id="sequence_type_lot">
            <field name="name">Lot</field>
        </record>
        <record model="ir.sequence.type-res.group" id="sequence_type_stock_group_admin">
            <field name="sequence_type" ref="sequence_type_lot"/>
            <field name="group" ref="res.group_admin"/>
        </record>
        <record model="ir.sequence.type-res.group" id="sequence_type_stock_group_stock_admin">
            <field name="sequence_type" ref="sequence_type_lot"/>
            <field name="group" ref="stock.group_stock_admin"/>
        </record>

        <record model="ir.sequence" id="sequence_lot">
            <field name="name">Lot</field>
            <field name="sequence_type" ref="stock_lot.sequence_type_stock_lot"/>
        </record>
    </data>
</tryton>
